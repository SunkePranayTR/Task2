name: Packer Full flow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  packer-full-flow:
    runs-on: winRunner1

    steps:
      - name: test flow
        uses: actions/checkout@v2

# name: test ansible connectivity

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:

# jobs:
#   test-connectivity:
#     runs-on: ansible_runner1

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Run ansible playbook
#         env:
#           JFROG_UNAME: ${{ secrets.JFROG_USERNAME }}
#           JFROG_PASSWORD: ${{ secrets.JFROG_PASSWORD }}
#         run: |
#           ansible-playbook \
#             -i ansible/inventory/dev/hosts.ini \
#             ansible/playbooks/connect.yml \
#             -vvv
